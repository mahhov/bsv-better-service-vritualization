{
  "name": "bsv-better-service-virtualization",
  "version": "0.1.5",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch src --watch spec --exec node spec/temp-test.js",
    "postversion": "git push && git push --tags && npm publish"
  },
  "author": "mahhov",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/mahhov/bsv-better-service-vritualization"
  },
  "devDependencies": {
    "nodemon": "^1.17.2"
  }
}
